# 자바스크립트 변수

JavaScript에서 변수를 선언하는 키워드는 `var`, `let`, `const` 3가지가 있다. 이들의 가장 큰 차이점은 **"유효 범위(Scope)"와 "값 변경 가능 여부"** 이다.

## `const` (Constant, 상수)
- 특징 : 한번 값을 넣으면 절대 바꿀 수 없다. (재할당 금지)
  
- 용도 : React 컴포넌트, 함수, 설정값, 바뀌지 않는 데이터 등 대부분의 경우에 사용이 가능하다.
- 주의 : 변수 자체를 다른 걸로 교체할 수는 없지만, **배열이나 객체 안의 내용물은 변경 가능**하다.

```js
const name = "성재";
name = "성재"; // ❌ 에러 발생! (Assignment to constant variable)

// 단, 객체/배열 내부 수정은 가능하다.
const user = { name: "성재" };
user.name = "철수"; // ✅ 가능 (메모리 주소는 그대로이기 때문)
```
## `let`
- 특징 : 값을 **자유롭게 변경(재할당)** 할 수 있다.
- 용도 : `for` 반복문의 `i` 변수나, 시간이 지나면 값이 변해야 하는 상태값 등을 다룰 때 사용합니다.

```js
let count = 1;
count = 2; // ✅ 가능
```

## `var` (Variable, 구식)
- 특징 : 아주 오래된 방식이다. 최신 개발 환경에서는 **"버그를 유발하는 주범"** 취급을 받는다고 한다.
- 문제점 : 

  1. 같은 이름으로 또 선언해도 에러가 안 난다.
  2.  `if` 문이나 `for` 문 블록 `{}` 을 무시하고 탈출합니다.

```js
var a = 1;
var b = 2; // 놀랍게도 에러가 안난다고 한다. (심각한 문제)

if (true) { 
    var b = 10;
}
console.log(b); // 10 출력됨 (블록 밖으로 탈출함)
```
## 가장 중요한 차이 : "블록 스코프" vs "함수 스코프"

`let`,`const`와 `var`의 결정적인 차이는 **"어디까지 살아남느냐(Scope)"** 이다.
- **블록 스코프 (`let`,`const`)** : 중괄호 `{ }`가 쳐진 구역(if문, for문 등) 안에서만 살아있다. 밖으로 못나감. (안전하다는 의미)
- 함수 스코프 (`var`) : 오직 **함수(Function)** 안에서만 갇혀있고, `if`나 `for` 같은 블록은 그냥 무시하고 전역 변수처럼 행동합니다. (위험하다는 의미)

```js
// 블록 스코프 예시
if (true) {
    let safe = "안전해";
    var dangerous = "위험해";
}

console.log(dangerous); // "위험해" 출력 (밖으로 새어 나온다)
console.log(safe);      // ❌ 에러! (ReferenceError: safe is not defined)
```

## 실무에서 사용할 때 사용 Tip!

1. 무조건 `const`를 먼저 쓴다. (기본값)
   - API 응답 데이터, 컴포넌트 이름, 함수 정의, Hook 리턴값 등.
2. 값이 진짜로 변해야 할 때만 `let`으로 바꾼다.
   - `for` 반복문, 사용자 입력에 따라 바뀌는 임시 변수 등.
3. `var`가 보이면 레거시(옛날) 코드라고 생각하면 된다.

