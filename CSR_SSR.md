# SSR 초기 렌더링 빠르게 하는 법

## CSR,SSR 차이

- ```CSR``` (Client-Side Rendering)은 클라이언트 측에서 렌더링 하는 방식 이다. CSR 방식은 페이지를 로드할 때 서버에서 모든 데이터를 받아와서 자바스크립트로 화면을 그리는 방식으로, 초기 로딩 속도는 느리지만 그 이후의 네이게이션은 빠르다.

- ```SSR``` (Server-Side Rendering)은 서버 축에서 렌더링하는 방식이다. SSR 방식은 페이지를 로드할 때 서버에서 먼저 렌더링하여 HTML 파일을 만들어서 보내주는 방식으로, 초기 로딩 속도는 빠르지만 그 이후의 네비게이션은 느립니다. 높이기 위한 SSR 방식과 그 이후의 네비게이션 속도를 높이기 위한 CSR 방식을 모두 지원한다.

https://toss.tech/article/faster-initial-rendering

## SSR의 과정은 **서버 단계**와 **클라이언트 단계**로 나뉜다.

### - 1 단계 : 서버에서 HTML 생성 (Rendering)

사용자가 웹 사이트에 접속 요청을 보내면, 서버는 다음 작업을 수행한다.

1. **데이터 페칭** : DB나 API에서 필요한 데이터를 가져온다.
2. **HTML 변환** : React 컴포넌트와 가져온 데이터를 합쳐서 순수한 HTML 문자열로 변환한다.

### - 2 단계 : 완성된 HTML을 브라우저로 보낸다.

1. **사용자 경험** : 브라우저는 이 HTML을 받자마자 화면에 그림을 그린다. 사용자는 자바스트립트가 다운로드되기도 전에 **완성된 콘텐츠를 즉시 볼 수 있다. (First Contentful Paint, FCP)**.
2. 하지만 이때 화면은 보이지만, 버튼을 클릭해도 아무 반응이 없다. 왜냐하면 아직 React 로직(자바스크립트)이 연결되지 않았기 때문.

### - 3단계 : 자바스크립트 번들 다운로드 & 실행

브라우저는 HTML을 렌더링하면서 백그라운드에서 React 라이브러리와 애플리케이션 코드가 담긴 **JS 번들 파일**을 다운로드하고 실행한다.

### - 4단계 : 하이드레이션 (Hydration) **핵심 ✅**

가장 중요한 단계이다. 다운로드된 React가 이미 그려진 HTML(DOM)을 인식하고, 그 위에 **이벤트 리스너**를 부착합니다.

1. React는 "어? 서버가 이미 HTML을 그려놨잖아??" 라고 인식하고, 처음부터 다시 그리는 대신 **기존 HTML에 기능만 연결**한다.
2. 이 과정을 **Hydration(수분 공급)**이라고 부른다. 건조한 HTML에 상호 작용이라는 물을 주는 것이다.
3. 이 작업이 끝나면 비로소 버튼 클릭 등의 인터렉션이 가능하다. (Time to Interactive, TTI)

# Next.js 를 사용하면 SSR 구현이 더 편리한 이유

## 1. 설정 없는 SSR 환경 조성

- **React만 사용할 때** : SSR 구현을 하려면 Node.js 서버를 직접 만들고, ```ReactDOMServer``` 설정을 하고, 클라이언트와 서버의 번들링 설정을 따로 관리해야 한다.
- **Next.js** : 설치하여 사용하자마자 SSR이 자동으로 된다. ```npm run dev```를 입력하여 실행하면 서버 설정, 번들링, TS 컴파일 설정을 알아서 해줌.

## 2. 페이지 단위 렌더링 방식 선택 가능 (Hybrid Rendering)
직접 구현하면 모든 페이지를 SSR로 할지 CSR로 할지 전역적으로 결정하기 쉽다. 하지만 Next.js는 **페이지 별로** 방식을 다르게 구현할 수 있다.

### 예
 - 메인페이지 : 검색 엔전 노출이 중요하니 ```SSR```
 - 마이 페이지 : 개인 정보가 중요하고 검색될 필요가 없으니 ```CSR```
 - 블로그 글 : 내용이 변하지 않으니 정적 사이트 생성 ```SSG```

## 3. 파일 시스템 기반 라우팅
- **React만 사용할 때** : ```react-router-dom``` 같은 라이브러리를 설치하고, 
```
<Route path="/Home" element={<Home />}
```
처럼 코드로 일일이 경로를 지정해야 한다.

- Next.js : ```app```(또는 ```page```) 폴더 안에 ```home.tsx```파일을 만들면 자동으로 ```/home``` 주소로 연결이 된다. 
- **폴더 구조는 곧 사이트 지도**가 되기 때문에 직관적이고 관리가 편함.
