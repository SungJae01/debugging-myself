# 웹 개발의 3대 요소

웹 개발의 가장 기초가 되는 **3대 요소(HTML, CSS, JS)**가 있다.

## 1. HTML (HyperText Markup Language)
**"웹 페이지의 뼈대(Structure)"** 이다.

- 역할 : 웹 페이지의 구조와 내용을 담당한다. 제목, 문단, 이미지, 버튼 등이 무엇인지 브라우저에게 알려주는 마크업 언어이다.
- 특징 : 프로그래밍 언어가 아니며, 논리적인 구조를 잡는데 집중한다.

# 시멘틱 태그 (Semantic Tags)
HTML5에서 도입된 개념으로, **의미가 있는(Semantic) 태그** 를 말합니다. 옛날에는 `<div>` 태그(의미 없음)만 사용하여 레이아웃을 짯다면, 지금은 해당 영역이 어떤 역할을 하는지 명확하게 나타내는 태그를 사용합니다.

1. 주요 시멘틱 종류
   - `<header>` : 페이지나 섹션의 머리말 (로고, 메뉴 등)
   - `<nav>` : 내비게이션 링크 모음 (메뉴바)
   - `main` : 문서의 핵심 내용 (페이지당 1개 권장)
   - `section` : 주제별 곤텐츠 영역
   - `article` : 독립적으로 배포 가능한 콘텐츠 (블로그 글, 뉴스 기사)
   - `aside` : 본문 외의 내용 (사이드바, 광고)
   - `footer` : 페이지의 바닥글 (저작권, 연락처)

2. 시멘틱 태그를 쓰는 이유

- 1. SEO(검색 엔진 최적화) : 구글 같은 검색 엔진 봇은 코드를 읽어서 페이지 내용을 파악한다. `<div>` 로만 되어있으면 어디가 중요한 제목이고 본문인지 모른다. `<article>` 이나 `<h1>` 등을 사용하면 검색엔진이 **"아! 이게 핵심 내용이구나"** 라고 이해하여 검색 결과를 상위에 노출될 확률이 높아진다.
- 2. 웹 접근성 (Accessibillty) : 시각 장애인이 사용하는 '스크린 리더'는 시멘틱 태그를 통해 페이지 구조를 파악한다. "네비게이션 영역입니다.", "본문입니다." 🔊 라고 안내해 줄 수 있어서 웹 평등성을 보장한다.
- 3. 코드 가독성과 유지보수 유리 : 개발자가 코드를 볼 때도 `<div class="top">` 보다 `<header>` 라고 적혀 있는 것이 훨씬 직관적이다.

3. 코드 비교

**나쁜 예**
```xml
<div class="header">로고 및 메뉴</div>
<div class="contents">
  <div class="news">뉴스 기사</div>
</div>
<div class="footer">저작권 정보</div>
```
**좋은 예 (Semantic)**
```xml
<header>로고 및 메뉴</header>
<main>
  <article>뉴스 기사</article>
</main>
<footer>저작권 정보</footer>
```

## 2. CSS (Cascading Style Sheet)

 **웹 페이지의 디자인(Presentation)** 이다.

 - 역할 : HTML로 만들어진 뼈대 위에 색상, 크기, 배치, 폰트 등 스타일을 입힌다.
 - 특징 : HTML과 분리하여 디자인을 관리함으로써 유지보수성을 높힌다.

### CSS 기초 문법

```css
/* 선택자 { 속성 : 값; } */
h1 {
    color: blue;
    font-size: 24px;
}
```
### 1. 우선순위

스타일이 겹칠 때 적용되는 순서이다.

1. `!important` (웬만하면 사용하지 말 것)
2. 인라인 스타일 (`style="..."`)
3. ID 선택자 (`#id`)
4. 클래스 선택자 (`.class`), 가상 클래스 (`:hover`)
5. 태그 선택자 (`<div>`,`<p>`)

### 2. 박스 모델
모든 HTML 요소는 박스 형태를 가진다. 레이아웃을 잡을 때 가장 기본이 되는 개념이다.

- Content : 텍스트나 이미지가 들어가는 실질적인 영역
- Padding : 테두리(Border)와 내용(Content) 사이의 안쪽 여백
- Border : 박스의 테두리
- Margin : 테두리(Border) 바깥의 여백 (요소와 요소 사이의 간격)

### 📌 실무에서 필수로 설정하면 좋음

기본적으로 padding과 border는 요소의 크기를 늘린다. 이를 방지하고 **설정한 witdh/height 안에 padding/border를 포함**시키기 위해 아래 코드를 전역에 적용한다.

```css
* {
    box-sizing : border-box; /* 필수! */
}
```

### 3. 레이아웃 (Layout) -Flex & Grid

과거 `float` 를 쓰던 방식에서 벗어나 현재 실무에서는 **Flexbox** 와 **Grid**를 표준으로 사용한다.



## 3. JavaScript (JS)

**"웹 페이지의 동작(Behavior)"** 이다.

- 역할 : 홈페이지가 살아 움직이게 한다. 사용자의 클릭에 반응하거나, 서버에서 데이터를 가져오거나, 화면을 동적으로 변화시키는 프로그래밍 언어이다.
- 특징 : 웹 브라우저에서 실행되는 유일한 프로그래밍 언어이다.

